# Firebase Configuration
# Se carga automáticamente desde firebase-key.json en la raíz del proyecto

# Frontend
VUE_APP_API_URL=http://localhost:8000

# Backend (uvicorn)
# HOST=127.0.0.1
# PORT=8000
